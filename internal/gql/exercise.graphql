input CreateExerciseInput {
  name: String!
  image: ImageInput
  howTo: String
  userID: ID
  musclesGroupIDs: [ID!]
  exerciseTypeIDs: [ID!]
}

extend type Mutation {
  createExercise(input: CreateExerciseInput!): Exercise
}
