// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/sahidrahman404/gigachad-api/ent/exercise"
	"github.com/sahidrahman404/gigachad-api/ent/exercisetype"
	"github.com/sahidrahman404/gigachad-api/ent/predicate"
	"github.com/sahidrahman404/gigachad-api/ent/schema/pksuid"
)

// ExerciseTypeQuery is the builder for querying ExerciseType entities.
type ExerciseTypeQuery struct {
	config
	ctx                *QueryContext
	order              []exercisetype.OrderOption
	inters             []Interceptor
	predicates         []predicate.ExerciseType
	withExercises      *ExerciseQuery
	modifiers          []func(*sql.Selector)
	loadTotal          []func(context.Context, []*ExerciseType) error
	withNamedExercises map[string]*ExerciseQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExerciseTypeQuery builder.
func (etq *ExerciseTypeQuery) Where(ps ...predicate.ExerciseType) *ExerciseTypeQuery {
	etq.predicates = append(etq.predicates, ps...)
	return etq
}

// Limit the number of records to be returned by this query.
func (etq *ExerciseTypeQuery) Limit(limit int) *ExerciseTypeQuery {
	etq.ctx.Limit = &limit
	return etq
}

// Offset to start from.
func (etq *ExerciseTypeQuery) Offset(offset int) *ExerciseTypeQuery {
	etq.ctx.Offset = &offset
	return etq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (etq *ExerciseTypeQuery) Unique(unique bool) *ExerciseTypeQuery {
	etq.ctx.Unique = &unique
	return etq
}

// Order specifies how the records should be ordered.
func (etq *ExerciseTypeQuery) Order(o ...exercisetype.OrderOption) *ExerciseTypeQuery {
	etq.order = append(etq.order, o...)
	return etq
}

// QueryExercises chains the current query on the "exercises" edge.
func (etq *ExerciseTypeQuery) QueryExercises() *ExerciseQuery {
	query := (&ExerciseClient{config: etq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := etq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := etq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exercisetype.Table, exercisetype.FieldID, selector),
			sqlgraph.To(exercise.Table, exercise.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, exercisetype.ExercisesTable, exercisetype.ExercisesPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(etq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ExerciseType entity from the query.
// Returns a *NotFoundError when no ExerciseType was found.
func (etq *ExerciseTypeQuery) First(ctx context.Context) (*ExerciseType, error) {
	nodes, err := etq.Limit(1).All(setContextOp(ctx, etq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{exercisetype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (etq *ExerciseTypeQuery) FirstX(ctx context.Context) *ExerciseType {
	node, err := etq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExerciseType ID from the query.
// Returns a *NotFoundError when no ExerciseType ID was found.
func (etq *ExerciseTypeQuery) FirstID(ctx context.Context) (id pksuid.ID, err error) {
	var ids []pksuid.ID
	if ids, err = etq.Limit(1).IDs(setContextOp(ctx, etq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{exercisetype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (etq *ExerciseTypeQuery) FirstIDX(ctx context.Context) pksuid.ID {
	id, err := etq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExerciseType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExerciseType entity is found.
// Returns a *NotFoundError when no ExerciseType entities are found.
func (etq *ExerciseTypeQuery) Only(ctx context.Context) (*ExerciseType, error) {
	nodes, err := etq.Limit(2).All(setContextOp(ctx, etq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{exercisetype.Label}
	default:
		return nil, &NotSingularError{exercisetype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (etq *ExerciseTypeQuery) OnlyX(ctx context.Context) *ExerciseType {
	node, err := etq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExerciseType ID in the query.
// Returns a *NotSingularError when more than one ExerciseType ID is found.
// Returns a *NotFoundError when no entities are found.
func (etq *ExerciseTypeQuery) OnlyID(ctx context.Context) (id pksuid.ID, err error) {
	var ids []pksuid.ID
	if ids, err = etq.Limit(2).IDs(setContextOp(ctx, etq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{exercisetype.Label}
	default:
		err = &NotSingularError{exercisetype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (etq *ExerciseTypeQuery) OnlyIDX(ctx context.Context) pksuid.ID {
	id, err := etq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExerciseTypes.
func (etq *ExerciseTypeQuery) All(ctx context.Context) ([]*ExerciseType, error) {
	ctx = setContextOp(ctx, etq.ctx, "All")
	if err := etq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExerciseType, *ExerciseTypeQuery]()
	return withInterceptors[[]*ExerciseType](ctx, etq, qr, etq.inters)
}

// AllX is like All, but panics if an error occurs.
func (etq *ExerciseTypeQuery) AllX(ctx context.Context) []*ExerciseType {
	nodes, err := etq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExerciseType IDs.
func (etq *ExerciseTypeQuery) IDs(ctx context.Context) (ids []pksuid.ID, err error) {
	if etq.ctx.Unique == nil && etq.path != nil {
		etq.Unique(true)
	}
	ctx = setContextOp(ctx, etq.ctx, "IDs")
	if err = etq.Select(exercisetype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (etq *ExerciseTypeQuery) IDsX(ctx context.Context) []pksuid.ID {
	ids, err := etq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (etq *ExerciseTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, etq.ctx, "Count")
	if err := etq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, etq, querierCount[*ExerciseTypeQuery](), etq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (etq *ExerciseTypeQuery) CountX(ctx context.Context) int {
	count, err := etq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (etq *ExerciseTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, etq.ctx, "Exist")
	switch _, err := etq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (etq *ExerciseTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := etq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExerciseTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (etq *ExerciseTypeQuery) Clone() *ExerciseTypeQuery {
	if etq == nil {
		return nil
	}
	return &ExerciseTypeQuery{
		config:        etq.config,
		ctx:           etq.ctx.Clone(),
		order:         append([]exercisetype.OrderOption{}, etq.order...),
		inters:        append([]Interceptor{}, etq.inters...),
		predicates:    append([]predicate.ExerciseType{}, etq.predicates...),
		withExercises: etq.withExercises.Clone(),
		// clone intermediate query.
		sql:  etq.sql.Clone(),
		path: etq.path,
	}
}

// WithExercises tells the query-builder to eager-load the nodes that are connected to
// the "exercises" edge. The optional arguments are used to configure the query builder of the edge.
func (etq *ExerciseTypeQuery) WithExercises(opts ...func(*ExerciseQuery)) *ExerciseTypeQuery {
	query := (&ExerciseClient{config: etq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	etq.withExercises = query
	return etq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExerciseType.Query().
//		GroupBy(exercisetype.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (etq *ExerciseTypeQuery) GroupBy(field string, fields ...string) *ExerciseTypeGroupBy {
	etq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExerciseTypeGroupBy{build: etq}
	grbuild.flds = &etq.ctx.Fields
	grbuild.label = exercisetype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ExerciseType.Query().
//		Select(exercisetype.FieldName).
//		Scan(ctx, &v)
func (etq *ExerciseTypeQuery) Select(fields ...string) *ExerciseTypeSelect {
	etq.ctx.Fields = append(etq.ctx.Fields, fields...)
	sbuild := &ExerciseTypeSelect{ExerciseTypeQuery: etq}
	sbuild.label = exercisetype.Label
	sbuild.flds, sbuild.scan = &etq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExerciseTypeSelect configured with the given aggregations.
func (etq *ExerciseTypeQuery) Aggregate(fns ...AggregateFunc) *ExerciseTypeSelect {
	return etq.Select().Aggregate(fns...)
}

func (etq *ExerciseTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range etq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, etq); err != nil {
				return err
			}
		}
	}
	for _, f := range etq.ctx.Fields {
		if !exercisetype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if etq.path != nil {
		prev, err := etq.path(ctx)
		if err != nil {
			return err
		}
		etq.sql = prev
	}
	return nil
}

func (etq *ExerciseTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExerciseType, error) {
	var (
		nodes       = []*ExerciseType{}
		_spec       = etq.querySpec()
		loadedTypes = [1]bool{
			etq.withExercises != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExerciseType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExerciseType{config: etq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(etq.modifiers) > 0 {
		_spec.Modifiers = etq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, etq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := etq.withExercises; query != nil {
		if err := etq.loadExercises(ctx, query, nodes,
			func(n *ExerciseType) { n.Edges.Exercises = []*Exercise{} },
			func(n *ExerciseType, e *Exercise) { n.Edges.Exercises = append(n.Edges.Exercises, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range etq.withNamedExercises {
		if err := etq.loadExercises(ctx, query, nodes,
			func(n *ExerciseType) { n.appendNamedExercises(name) },
			func(n *ExerciseType, e *Exercise) { n.appendNamedExercises(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range etq.loadTotal {
		if err := etq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (etq *ExerciseTypeQuery) loadExercises(ctx context.Context, query *ExerciseQuery, nodes []*ExerciseType, init func(*ExerciseType), assign func(*ExerciseType, *Exercise)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[pksuid.ID]*ExerciseType)
	nids := make(map[pksuid.ID]map[*ExerciseType]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(exercisetype.ExercisesTable)
		s.Join(joinT).On(s.C(exercise.FieldID), joinT.C(exercisetype.ExercisesPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(exercisetype.ExercisesPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(exercisetype.ExercisesPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(pksuid.ID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*pksuid.ID)
				inValue := *values[1].(*pksuid.ID)
				if nids[inValue] == nil {
					nids[inValue] = map[*ExerciseType]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Exercise](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "exercises" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (etq *ExerciseTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := etq.querySpec()
	if len(etq.modifiers) > 0 {
		_spec.Modifiers = etq.modifiers
	}
	_spec.Node.Columns = etq.ctx.Fields
	if len(etq.ctx.Fields) > 0 {
		_spec.Unique = etq.ctx.Unique != nil && *etq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, etq.driver, _spec)
}

func (etq *ExerciseTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(exercisetype.Table, exercisetype.Columns, sqlgraph.NewFieldSpec(exercisetype.FieldID, field.TypeString))
	_spec.From = etq.sql
	if unique := etq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if etq.path != nil {
		_spec.Unique = true
	}
	if fields := etq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, exercisetype.FieldID)
		for i := range fields {
			if fields[i] != exercisetype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := etq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := etq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := etq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := etq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (etq *ExerciseTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(etq.driver.Dialect())
	t1 := builder.Table(exercisetype.Table)
	columns := etq.ctx.Fields
	if len(columns) == 0 {
		columns = exercisetype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if etq.sql != nil {
		selector = etq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if etq.ctx.Unique != nil && *etq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range etq.predicates {
		p(selector)
	}
	for _, p := range etq.order {
		p(selector)
	}
	if offset := etq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := etq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedExercises tells the query-builder to eager-load the nodes that are connected to the "exercises"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (etq *ExerciseTypeQuery) WithNamedExercises(name string, opts ...func(*ExerciseQuery)) *ExerciseTypeQuery {
	query := (&ExerciseClient{config: etq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if etq.withNamedExercises == nil {
		etq.withNamedExercises = make(map[string]*ExerciseQuery)
	}
	etq.withNamedExercises[name] = query
	return etq
}

// ExerciseTypeGroupBy is the group-by builder for ExerciseType entities.
type ExerciseTypeGroupBy struct {
	selector
	build *ExerciseTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (etgb *ExerciseTypeGroupBy) Aggregate(fns ...AggregateFunc) *ExerciseTypeGroupBy {
	etgb.fns = append(etgb.fns, fns...)
	return etgb
}

// Scan applies the selector query and scans the result into the given value.
func (etgb *ExerciseTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, etgb.build.ctx, "GroupBy")
	if err := etgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExerciseTypeQuery, *ExerciseTypeGroupBy](ctx, etgb.build, etgb, etgb.build.inters, v)
}

func (etgb *ExerciseTypeGroupBy) sqlScan(ctx context.Context, root *ExerciseTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(etgb.fns))
	for _, fn := range etgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*etgb.flds)+len(etgb.fns))
		for _, f := range *etgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*etgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := etgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExerciseTypeSelect is the builder for selecting fields of ExerciseType entities.
type ExerciseTypeSelect struct {
	*ExerciseTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ets *ExerciseTypeSelect) Aggregate(fns ...AggregateFunc) *ExerciseTypeSelect {
	ets.fns = append(ets.fns, fns...)
	return ets
}

// Scan applies the selector query and scans the result into the given value.
func (ets *ExerciseTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ets.ctx, "Select")
	if err := ets.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExerciseTypeQuery, *ExerciseTypeSelect](ctx, ets.ExerciseTypeQuery, ets, ets.inters, v)
}

func (ets *ExerciseTypeSelect) sqlScan(ctx context.Context, root *ExerciseTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ets.fns))
	for _, fn := range ets.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ets.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ets.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
